#include <stdio.h>

int main() {
    int a, b;

    // Input two integers
    scanf("%d %d", &a, &b);

    // Perform and print the bitwise OR operation
    printf("b<<1 = %u\n", b << 1)
    return 0;
}
